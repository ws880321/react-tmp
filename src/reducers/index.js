import { combineReducers } from 'redux'
// import { visibilityFilter, todos } from './todo'
import { getHotNovel } from './navel'


const rootReducer = combineReducers({
    getHotNovel
})

export default rootReducer